/// The Firebase project's auto-generated "web" OAuth client ID — from
/// android/app/google-services.json's `client_type: 3` entry. This is what
/// makes `GoogleSignIn` on Android hand back an ID token whose audience
/// matches the Firebase project, which is what the backend's
/// google_auth.verify_google_id_token expects (same project as the web app).
const googleServerClientId = '1019199433165-lk8ss4i2ejl2uunfkjer9vjdtg4bovag.apps.googleusercontent.com';
